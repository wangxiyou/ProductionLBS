using System;
using System.Collections.Generic;
using System.Text;
using Xunit;

namespace PB.PLBS.Domains.Test
{
    public class FormulaTest
    {
        [Fact]
        public void CreateAndCopyTest()
        {
            //string code = "F1";

            ///// 使用空物料编号创建一个对象
            //Formula task = null;
            //try
            //{
            //    task = Formula.Create(null);
            //}
            //catch (DomainExcetption ex)
            //{
            //    Assert.NotNull(ex);
            //}
            //try
            //{
            //    task = Formula.Create(string.Empty);
            //}
            //catch (DomainExcetption ex)
            //{
            //    Assert.NotNull(ex);
            //}

            ///// 使用编号创建一个对象
            //task = Formula.Create(code);
            //Assert.NotNull(task);
            //FormulaItem item = FormulaItem.Create("Bom1", "M1", 50);
            //task.AddFormulaItem(item);
        }
    }
}
